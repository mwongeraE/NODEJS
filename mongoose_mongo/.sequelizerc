const path = require('path')

module.exports = {
  "config": path.resolve('./server/config', 'config.json'),
  "models-path": path.resolve('./server/models'),
  "migrations-path": path.resolve('./server/migrations')
}

/*
this file is going to bootstrap our application with the above paths.
the config.json file is going to contain the configuration for our application
The models will contain our application models
migrations will contain the different migrations for our application.
The migrations files are used to create our apps table using the models.
*/
